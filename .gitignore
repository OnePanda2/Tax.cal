# dependencies
node_modules/

# OS / editor junk
Thumbs.db
desktop.ini
.DS_Store
*.swp

# Third-party material — not ours to republish in a public repo.
dissectmac-international-users-playbook.pdf

# Private strategy notes — kept locally, not published.
taxmirror-office-hours-design-doc.md
taxmirror_handover_prompt.md

# Cloudflare Worker local state
api/.wrangler-test/
api/.wrangler/

# Full project report & handover — strategy, pricing, legal and security notes.
# The repo is PUBLIC, so this must never be committed. The canonical copy lives
# OUTSIDE this folder, at F:\Projects\PROJECT-HANDOVER TaxCal.md, precisely so a
# stray `git add -A` cannot publish it. These patterns are a safety net in case a
# copy ever lands in here under a slightly different name.
*HANDOVER*.md
*handover*.md
